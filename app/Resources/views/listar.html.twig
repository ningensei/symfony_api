{% extends "base.html.twig" %}

{% block content %}
    <div class="container-fluid">

        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">
                    {{ current|capitalize }}
                </h1>
                <ol class="breadcrumb">
                    <li>
                        <i class="fa fa-dashboard"></i>  <a href="{{ url('home') }}">Dashboard</a>
                    </li>
                    <li class="active">
                        <i class="fa fa-table"></i> {{ current|capitalize }}
                    </li>
                </ol>
            </div>
        </div>
        <!-- /.row -->
            
        <div class="row">
            <div class="col-xs-12">
                <h2>Lista de empresas</h2>
                <div class="table-responsive">
                    <table id="listaEmpresas" class="table table-hover table-striped table-bordered">
                        <thead>
                                <th>Id</th>
                                <th>Nombre</th>
                                <th>Dirección</th>
                                <th>Cuit</th>
                                <th>Opciones</th>
                        </thead>
                        <tbody>
                            <tr><td colspan="5"></td><tr>
                        </tbody>
                    </table>

                    <div class="text-center"><a href="{{ url('agregar') }}"><button type="button" class="btn btn-lg btn-primary">Agregar Empresa</button></a></div>
                </div>
            </div>

        </div>
        <!-- /.row -->

    </div>
    <!-- /.container-fluid -->
{% endblock %}